class Solution {
  public:
    int totalCount(int k, vector<int>& arr) {
        // code here
        int totalDivisions = 0;
        for(int num : arr) {
            totalDivisions += (num + k - 1) / k;
        }
        return totalDivisions;
    }
};
